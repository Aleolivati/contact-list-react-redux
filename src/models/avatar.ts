class Avatars {
  id: number
  label: string
  icon: string

  constructor(id: number, label: string, icon: string) {
    this.id = id
    this.label = label
    this.icon = icon
  }
}

export default Avatars
